// Created by  Ambesh kumar
#include "bits/stdc++.h"

//I already compile this file in my computer system to save the time
//if get the compiler error thhen you should change this header to
// #include "bits/stdc++.h"  ---> #include <bits/stdc++.h>

#define  ll  long long
# define  loop(x,n)  for(int x = 0; x < n; ++x)
#define   constloop(x,a,n) for(int x = a; x < n; ++x)
#define  revloop(x,a,n)  for(int x = a; x > n; x--)

// ------- above define macros---------------------//



using namespace std;


//--------------------------------------------------------------------


// Time complexity O(nlogn) and Space complexity O(1)


void Three_max_number(int arr[], int n)
{

	sort(arr, arr + n);

	cout << " First_max " << arr[n - 1] << " Second_max " << arr[n - 2] << " Third_max " << arr[n - 3] << "\n";



}


//--------------------------------------------------------------------


// Time complexity O(nlogn) and Space complexity O(1)


void Three_Max_number(int arr[], int n)
{




}









int main()
{

	ios::sync_with_stdio(0);
	cin.tie(0);




	return 0;
}